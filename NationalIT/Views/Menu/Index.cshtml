@model IEnumerable<NationalIT.mMenu>
<div class="row-fluid">
    <div class="span12">
        <div class="box">
            <div class="box-title">
                <h3>
                    <i class="icon-table"></i>Menu managerment</h3>
            </div>
            <div class="box-content">
                <p style="margin-left: 7px;">                    
                    @Html.ActionLink("Add Menu", "Create", null, new { @class = "btn btn-primary" })
                </p>
                <table class="table">
                    <thead>
                        <tr class="table-flag-blue">
                            <th>
                                Menu name
                            </th>
                            <th>
                                Loction
                            </th>
                            <th>
                                Status
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.ActionLink(item.Title, "Edit", new { id = item.ID }, null)
                                </td>
                                <td>
                                    @item.Oder
                                </td>
                                <td>
                                    <a href="#" class="IsActive" getID="@item.ID">
                                        @if (item.IsActive == true)
                                        { <span>Actived</span>}
                                        else
                                        {<span class='validation-summary-errors'>UnActived</span>}</a>
                                </td>
                                <td>
                                    @Html.ActionLink("Delete", "Delete", new { id = item.ID }, new { @class = "confirmDelete" })
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

